# Raw event and user data — not committed (large, sensitive)
raw-data/*.csv

# Generated data — produced by running the notebooks
data/processed/*
!data/processed/.gitkeep

# Map — private per-event data kept local; only the aggregate is published.
# Published files: events_aggregated.geojson, color_params.json, preview_small_all.png
data/map/coords_lookup.pkl
data/map/events.geojson

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.env
.venv/
venv/

# Jupyter
.ipynb_checkpoints/

# Jupyter Book build output
_build/

# Old working files — development notebooks, superseded by the clean pipeline
Old Code/

# Internal tooling
openspec/
.claude/
